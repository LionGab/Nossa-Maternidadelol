Você é um Engenheiro Front-end Sênior. Aplique as mudanças abaixo sem instalar libs novas e sem quebrar o build. Objetivo: deixar a Home pronta para avaliação (promessa clara, 1 CTA principal, LCP/CLS bons) e criar uma rota /demo clicável.

0) Detecte o stack e siga o caminho correto

Se for Next.js App Router: use app/**.

Se for Next.js Pages Router: use pages/**.

Se for Vite/CRA: edite src/App.tsx e crie componentes em src/components/**.

Se não houver Tailwind, use classes utilitárias simples no CSS existente (sem instalar nada).

1) Home — Hero acima da dobra (substituir o topo atual)

Copy (não altere):

Título: Acolhimento, orientação e rotina leve — em 5 minutos por dia.

Subtítulo: Criado com a Nathália Valente para quem quer menos culpa e mais apoio de verdade.

CTA primário: Entrar como Convidada → link para /demo

CTA secundário: Falar com a NathIA → link para /nathia (página simples placeholder se não existir)

Micro-garantias (linha de confiança): Sem julgamentos • Conteúdo feito por mães • Teste fechado

Implementação (condicional):

Next App Router: criar app/(marketing)/_components/Hero.tsx e usar em app/(marketing)/page.tsx.

Next Pages Router: componente components/Hero.tsx e usar em pages/index.tsx.

Vite/CRA: componente src/components/Hero.tsx e usar em src/App.tsx.

Inclua um mock de imagem no hero (/public/hero-mock.png se existir; se não, crie um placeholder leve) com width/height fixos e loading="eager" + fetchPriority="high". Garanta peso ≤120KB.

2) Seções logo abaixo (ordem enxuta)

Como funciona (3 passos)

Responda 5 perguntas

Receba um plano diário de 5 min

Conte com a NathIA

Benefícios (4 cards orientados a resultado)

Sono melhor • Menos estresse • Rede de apoio • Autocuidado possível
Cada card com 1 frase curta de outcome (sem jargão).

NathIA (mini-demo)

Um bloco com 3 exemplos de perguntas:

“Estou exausta, por onde começo hoje?”

“Como peço ajuda sem me sentir culpada?”

“Dica rápida para dormir melhor?”

Botão “Falar com a NathIA” → /nathia (criar página placeholder se não existir).

Depoimentos (placeholder): 2–3 bullets curtos (sem fotos).

FAQ mínimo (3 perguntas): acesso, privacidade, o que é o teste fechado.

Rodapé essencial: links para Privacidade, Termos, Contato e aviso “Teste fechado para avaliação”.

Crie componentes pequenos (ex.: HowItWorks, Benefits, NathIASection, Testimonials, FAQ, SiteFooter) e importe na Home.

3) Rota /demo (sem login)

Crie /demo com 3 telas estáticas navegáveis simulando o app:

Tela 1: “Plano diário de 5 min” (lista de 3 micro-tarefas)

Tela 2: “Check-in rápido” (humor/energia/sono em botões)

Tela 3: “Conversa com a NathIA” (mensagens dummy + input desabilitado)
Adicionar topo com botão “Voltar” para a Home.

4) Metadados e noindex (teste fechado)

Título: Nossa Maternidade — Acolhimento em 5 minutos/dia

Description curta coerente com o subtítulo.

Open Graph com /og.png (se não existir, gerar placeholder).

Adicionar noindex para o teste:

Next App Router: export const metadata = { robots: { index: false, follow: false } }

Pages Router / Vite/CRA: meta tag <meta name="robots" content="noindex, nofollow" /> no head.

5) Performance básica

Remover carrosséis/auto-slides do topo (se houver).

Usar fontes do sistema (remover imports de fontes pesadas externas nesta página).

Garantir CLS ~ 0 (definir width/height na imagem do hero).

Tamanho total da Home (acima da dobra) < 300KB.

6) Páginas auxiliares mínimas

/nathia: página simples com título “NathIA (prévia)” + parágrafo explicando que é uma prévia e um botão “Voltar para Home”.

7) Critérios de aceite (aceite somente quando todos passarem)

Home renderiza com 1 CTA principal (“Entrar como Convidada”) visível acima da dobra em desktop e mobile.

LCP local (DevTools Performance) < 2.5s em primeira visita.

Navegar para /demo funciona e percorre as 3 telas.

Existe FAQ e rodapé com os links citados.

Página marcada como noindex.

Nenhuma biblioteca nova adicionada.

8) Onde editar (resuma ao final o que foi criado/alterado)

Liste os arquivos criados/alterados (com caminhos) e um breve resumo das mudanças em cada um.

Importante: Não faça perguntas. Detecte o stack, aplique a solução correspondente e entregue tudo pronto.