# NAT-IA - Exemplos de Uso Completos

Este arquivo contém exemplos práticos de como usar cada módulo da NAT-IA em cenários reais.

---

## Cenário 1: Usuária Nova - Onboarding Completo

```typescript
import {
  getOnboardingQuestions,
  analisarRespostas,
  gerarStarterPack,
  chatEmpatico,
  createChatContext,
} from '@/services/nathia';

// 1. Obter perguntas do onboarding
const perguntas = getOnboardingQuestions();
// Exibir para usuária responder

// 2. Usuária respondeu, vamos analisar
const respostas = [
  {
    question_id: 'stage',
    answer: 'Segundo trimestre',
    timestamp: new Date(),
  },
  {
    question_id: 'concerns',
    answer: 'Saúde do bebê,Sono,Amamentação',
    timestamp: new Date(),
  },
  {
    question_id: 'support',
    answer: 'Tenho algum apoio',
    timestamp: new Date(),
  },
  {
    question_id: 'goals',
    answer: 'Entender melhor o que está acontecendo,Conectar com outras mães',
    timestamp: new Date(),
  },
];

const analise = await analisarRespostas(respostas);
console.log('Perfil criado:', analise.perfil);
// {
//   stage: "mid",
//   concerns: ["saude_bebe", "sono", "amamentacao"],
//   goals: ["entender_desenvolvimento", "conexao_social"],
//   ...
// }

// 3. Gerar starter pack personalizado
const pack = await gerarStarterPack(analise.perfil);

console.log('Mensagem de boas-vindas:', pack.welcome_message);
// "Você está no auge da gravidez! Vamos tornar essa fase ainda mais especial..."

console.log('Grupos recomendados:', pack.grupos);
// [
//   { name: "Mães do Segundo Trimestre", match_score: 0.85, ... },
//   { name: "Sono do Bebê", match_score: 0.75, ... }
// ]

console.log('Conteúdos iniciais:', pack.conteudo);
// [
//   { title: "Guia do Segundo Trimestre", match_score: 0.9, ... },
//   { title: "Preparando-se para a Amamentação", match_score: 0.8, ... }
// ]

// 4. Iniciar primeira conversa com NAT-IA
const contexto = createChatContext('user_123', {
  user_profile: analise.perfil,
});

const primeiraResposta = await chatEmpatico('Olá! Estou um pouco ansiosa com a gravidez', contexto);

console.log(primeiraResposta.resposta);
// "Olá! É completamente normal sentir ansiedade durante a gravidez..."
console.log(primeiraResposta.next_step);
// "Que tal explorar alguns exercícios de respiração?"
```

---

## Cenário 2: Detecção de Risco e Protocolo SOS

```typescript
import { chatEmpatico, classificarSentimento, detectarRisco, acionarSOS } from '@/services/nathia';

// Usuária envia mensagem preocupante
const mensagem = 'Não aguento mais essa situação, quero que tudo acabe';

// 1. Chat processa normalmente
const resposta = await chatEmpatico(mensagem, contexto);

// 2. Mas também fazemos triagem paralela
const sentimento = await classificarSentimento(mensagem);
console.log(sentimento);
// {
//   sentimento: "desespero",
//   intensidade: 9,
//   valence: "negative",
//   keywords: ["não aguento", "acabe"]
// }

const risco = await detectarRisco(mensagem);
console.log(risco);
// {
//   nivel: "risk",
//   sinais: ["Expressão de alto risco: quero que tudo acabe"],
//   confidence: 0.9,
//   requires_human_review: true
// }

// 3. Nível de risco alto - acionar SOS
if (risco.nivel === 'risk') {
  const sosResult = await acionarSOS('user_123', {
    mensagem,
    riskAssessment: risco,
  });

  console.log('SOS acionado:', sosResult);
  // {
  //   sent_to_moderation: true,
  //   resources_displayed: ["CVV", "SAMU", "LIGUE_180"],
  //   support_contacts: [
  //     { name: "CVV", phone: "188", description: "...", available_24_7: true },
  //     { name: "SAMU", phone: "192", ... }
  //   ]
  // }

  // 4. Exibir recursos imediatamente na UI
  showEmergencyResources(sosResult.support_contacts);

  // 5. Modificar resposta do chat para incluir apoio urgente
  resposta.resposta = `${resposta.resposta}\n\n⚠️ IMPORTANTE: Se você está em crise, por favor:\n- CVV (188): Apoio emocional 24h\n- SAMU (192): Emergências médicas\n\nVocê não está sozinha. Estamos aqui e há pessoas prontas para ajudar.`;
}
```

---

## Cenário 3: Curadoria de Conteúdo para Publicação

```typescript
import {
  resumirConteudo,
  criarCincoMinutos,
  gerarChecklist,
  simplificarLinguagem,
  identificarTermosTecnicos,
} from '@/services/nathia';

// Artigo original complexo
const artigoOriginal = `
A mastite puerperal é uma infecção do tecido mamário que ocorre
frequentemente durante a lactação. Caracteriza-se por sintomas
como hiperemia, edema, dor localizada e febre...
[continua por várias páginas]
`;

// 1. Identificar termos técnicos que precisam explicação
const termos = identificarTermosTecnicos(artigoOriginal);
console.log('Termos técnicos:', termos);
// ["mastite", "puerperal", "lactação", "hiperemia", "edema"]

// 2. Simplificar linguagem
const artigoSimplificado = await simplificarLinguagem(artigoOriginal);
console.log('Readability antes:', artigoSimplificado.readability_score_before); // 35
console.log('Readability depois:', artigoSimplificado.readability_score_after); // 65
console.log('Texto simplificado:', artigoSimplificado.simplified);
// "Mastite (inflamação da mama) é comum durante a amamentação.
//  Os sinais incluem vermelhidão, inchaço, dor e febre..."

// 3. Criar versões alternativas do conteúdo
const resumo = await resumirConteudo(artigoSimplificado.simplified);
console.log('Resumo (5 linhas):', resumo.resumo);
console.log('Tempo de leitura:', resumo.reading_time_minutes, 'minutos');

const cincoMin = await criarCincoMinutos(artigoSimplificado.simplified);
console.log('5 bullets práticos:', cincoMin.bullets);
// [
//   "Mastite é inflamação da mama, comum na amamentação",
//   "Sinais: vermelhidão, inchaço, dor, febre acima de 38°C",
//   "Continue amamentando - ajuda a drenar e melhorar",
//   "Compressas mornas antes e frias depois ajudam",
//   "Procure médico se sintomas piorarem em 24h"
// ]

const checklist = await gerarChecklist(artigoSimplificado.simplified);
console.log('Checklist:', checklist);
// {
//   title: "O que fazer se suspeitar de mastite",
//   items: [
//     { text: "Continuar amamentando frequentemente", is_critical: true, order: 1 },
//     { text: "Aplicar compressas mornas antes de amamentar", is_critical: false, order: 2 },
//     { text: "Aplicar compressas frias após amamentar", is_critical: false, order: 3 },
//     { text: "Descansar e hidratar-se bem", is_critical: false, order: 4 },
//     { text: "Observar febre e dor nas próximas 24h", is_critical: true, order: 5 },
//     { text: "Consultar médico se sintomas piorarem", is_critical: true, order: 6 }
//   ],
//   estimated_completion_time: "30 minutos"
// }

// 4. Publicar múltiplas versões
await publishContent({
  original: artigoSimplificado.simplified,
  resumo: resumo.resumo,
  cincoMinutos: cincoMin.bullets,
  checklist: checklist,
});
```

---

## Cenário 4: Moderação de Comentário em Círculo

```typescript
import { analisarMensagem, decidirAcao, sugerirReescrita, gerarRationale } from '@/services/nathia';

// Usuária postou comentário em círculo
const comentario = 'Você DEVERIA amamentar exclusivamente! Mães de verdade não dão fórmula.';

// 1. Análise completa de moderação
const analise = await analisarMensagem(comentario);

console.log('Análise:', analise);
// {
//   judgement_score: 0.85,
//   toxicity_score: 0.4,
//   is_safe: false,
//   concerns: ["Julgamento de escolhas maternas", "Julgamento sobre amamentação"],
//   suggested_rewrite: "...",
//   rationale: "..."
// }

// 2. Decisão automática
const acao = decidirAcao(analise);
console.log('Ação recomendada:', acao); // "reject"

if (acao === 'reject') {
  // Bloquear publicação automaticamente
  console.log('Comentário bloqueado automaticamente');

  // Mostrar para usuária o motivo e sugestão
  const reescrita = await sugerirReescrita(comentario);
  console.log('Sugestão de reescrita:', reescrita);
  // "Amamentação exclusiva funcionou muito bem para mim e meu bebê.
  //  Cada família faz o que é melhor para sua situação. Se quiser,
  //  posso compartilhar o que me ajudou com a amamentação!"

  // Exibir na UI
  showModerationFeedback({
    blocked: true,
    reason: 'Seu comentário contém linguagem julgamental',
    suggestion: reescrita,
    educational_note:
      'Nossa comunidade valoriza o respeito às diferentes escolhas maternas. Compartilhe sua experiência sem prescrever ou julgar.',
  });
} else if (acao === 'review') {
  // Enviar para moderadora humana revisar
  await sendToHumanReview({
    comment: comentario,
    analysis: analise,
    rationale: analise.rationale,
  });

  // Informar usuária
  showMessage('Seu comentário está em análise e será publicado em breve.');
} else {
  // Aprovar automaticamente
  await publishComment(comentario);
}
```

---

## Cenário 5: Sistema de Recomendações Personalizado

```typescript
import { recomendarConteudo, recomendarCirculo, recomendarHabito, rerankWithFeedback } from '@/services/nathia';

// Usuária abre o app
const userId = 'user_123';

// 1. Buscar contexto atual da usuária
const userContext = await getUserContext(userId);
// {
//   current_stage: "postpartum",
//   current_mood: "tired",
//   recent_activity: ["sono", "amamentacao", "exaustao"],
//   preferences: { stage: "postpartum", concerns: ["sono", "saude_mental"] }
// }

// 2. Recomendar conteúdo
const conteudos = await recomendarConteudo(userId, userContext);

console.log('Justificativa:', conteudos.justificativa);
// "Recomendamos conteúdos para sua fase atual (postpartum) e
//  baseado em seus interesses: sono, amamentacao"

console.log('Top 5 conteúdos:', conteudos.itens);
// [
//   {
//     title: "Sono do Recém-Nascido: O Que Esperar",
//     match_score: 0.92,
//     reason: "relacionado ao que você tem buscado, alinhado com sua fase atual"
//   },
//   {
//     title: "Autocuidado no Pós-Parto",
//     match_score: 0.85,
//     reason: "útil para como você está se sentindo agora"
//   },
//   ...
// ]

// 3. Recomendar círculos
const circulos = await recomendarCirculo(userId, userContext.preferences);

console.log('Top 3 círculos:', circulos.circulos);
// [
//   {
//     name: "Mães do Pós-Parto",
//     match_score: 0.88,
//     reason: "Outras mães na mesma fase que você"
//   },
//   {
//     name: "Sono e Rotina",
//     match_score: 0.82,
//     reason: "Comunidade focada em suas preocupações"
//   },
//   ...
// ]

// 4. Recomendar hábito (apenas 1 de cada vez)
const habito = await recomendarHabito(userId, userContext.preferences);

console.log('Hábito recomendado:', habito.habito);
// { title: "Journaling Materno", category: "saude_mental", ... }

console.log('Justificativa:', habito.justificativa);
// "Este hábito vai te ajudar a registrar pensamentos e emoções diariamente,
//  alinhado com seus objetivos de saude_mental"

console.log('Micro-objetivos:', habito.micro_objetivos);
// [
//   {
//     titulo: "Começar journaling",
//     passos: ["Definir horário do dia", "Preparar caderno", "Escrever 5 minutos"],
//     prazo_dias: 3
//   }
// ]

// 5. Ajustar com feedback da usuária
const feedback = [
  { id: 'content_1', action: 'like' },
  { id: 'content_2', action: 'click' },
  { id: 'content_3', action: 'ignore' },
];

const conteudosAjustados = rerankWithFeedback(conteudos.itens, feedback);
console.log('Conteúdos re-ranqueados:', conteudosAjustados);
// content_1 e content_2 sobem, content_3 desce
```

---

## Cenário 6: Coaching de Hábitos com Mensagens Motivacionais

```typescript
import {
  criarMicroObjetivo,
  registrarCompletude,
  trackProgresso,
  gerarMensagemMotivacional,
  gerarLembreteGentil,
  identificarBarreiras,
} from '@/services/nathia';

// 1. Usuária quer criar novo hábito
const objetivo = 'Quero começar a meditar para controlar a ansiedade';

const micro = await criarMicroObjetivo(objetivo);
console.log('Micro-objetivo criado:', micro);
// {
//   titulo: "Começar meditação com 5 minutos",
//   passos: [
//     "Escolher um momento específico do dia",
//     "Baixar app de meditação ou encontrar vídeos guiados",
//     "Começar pequeno, com 5 minutos por dia"
//   ],
//   prazo_dias: 7,
//   difficulty: "easy"
// }

// Salvar hábito
const habitId = await saveHabit(micro);

// 2. Usuária completa o hábito alguns dias
await registrarCompletude('user_123', habitId); // Dia 1
await registrarCompletude('user_123', habitId); // Dia 2
await registrarCompletude('user_123', habitId); // Dia 3

// 3. Verificar progresso
const progresso = await trackProgresso('user_123', habitId);
console.log('Progresso:', progresso);
// {
//   streak: 3,
//   completude: 15, // 15%
//   last_completed: Date (hoje),
//   total_completions: 3
// }

// 4. Gerar mensagem motivacional
const mensagem = gerarMensagemMotivacional(progresso);
console.log('Mensagem:', mensagem);
// {
//   mensagem: "3 dias seguidos! Você está construindo um hábito de verdade.",
//   tone: "celebrating",
//   avoid_comparison: true // NUNCA compara com outras mães
// }

// Exibir para usuária
showNotification(mensagem.mensagem);

// 5. Depois de alguns dias sem completar
// ... usuária não completou por 4 dias ...

const lembrete = gerarLembreteGentil(micro.habito);
console.log('Lembrete:', lembrete);
// "Oi! Que tal reservar alguns minutos para meditação hoje?
//  Não precisa ser perfeito, só começar já é ótimo."

// 6. Identificar possíveis barreiras
const barreiras = await identificarBarreiras('user_123', habitId);
console.log('Barreiras identificadas:', barreiras);
// [
//   {
//     barrier: "Dificuldade de manter consistência",
//     suggestion: "Que tal escolher um horário fixo e criar um lembrete?"
//   }
// ]

// Oferecer ajustes
showSuggestion({
  message: 'Percebi que está difícil manter a meditação diária.',
  suggestions: barreiras.map((b) => b.suggestion),
  actions: [
    { label: 'Ajustar horário', action: 'edit_schedule' },
    { label: 'Reduzir para 3x/semana', action: 'reduce_frequency' },
    { label: 'Escolher outro hábito', action: 'change_habit' },
  ],
});
```

---

## Cenário 7: Analytics e Dashboard (Sem PII)

```typescript
import {
  extrairRotulos,
  anonimizar,
  gerarMetricas,
  identificarTendencias,
  exportarParaDashboard,
  validarConformidadeLGPD,
} from '@/services/nathia';

// 1. Coletar interações ao longo do tempo
const interacoes = [];

// Para cada interação do usuário
async function processInteraction(userMessage: string) {
  // Extrair rótulos
  const labels = await extrairRotulos(userMessage);

  // Criar dados da interação
  const interactionData = {
    user_id: 'user_123', // Vai ser removido
    message: userMessage,
    labels,
    timestamp: new Date(),
  };

  // Anonimizar ANTES de salvar
  const dadosAnonimos = anonimizar(interactionData);

  // Validar conformidade
  const validacao = validarConformidadeLGPD(dadosAnonimos);
  if (!validacao.compliant) {
    console.error('Dados não conformes:', validacao.violations);
    return; // Não salvar
  }

  // Salvar dados anonimizados
  interacoes.push(dadosAnonimos);
}

// Processar várias mensagens
await processInteraction('Estou preocupada com o sono do bebê');
await processInteraction('Como faço para lidar com a ansiedade?');
await processInteraction('Estou muito feliz hoje!');
// ... mais interações ...

// 2. Gerar métricas agregadas (semanal)
const metricas = gerarMetricas(interacoes);

console.log('Métricas da semana:', metricas);
// {
//   topics_freq: {
//     sono: 45,
//     amamentacao: 32,
//     saude_mental: 28,
//     desenvolvimento: 15
//   },
//   sentiment_dist: {
//     positive: 0.35,
//     neutral: 0.45,
//     negative: 0.20
//   },
//   engagement: {
//     avg_session_length: 8.5,
//     interactions_per_session: 4.2,
//     return_rate: 0.68
//   },
//   time_range: { start: Date, end: Date }
// }

// 3. Comparar com semanas anteriores
const metricasAnteriores = [
  metricas_semana1,
  metricas_semana2,
  metricas_semana3,
  metricas, // semana atual
];

const tendencias = identificarTendencias(metricasAnteriores);
console.log('Tendências:', tendencias);
// {
//   trending_topics: ["saude_mental", "sono"],
//   sentiment_trend: "stable",
//   engagement_trend: "increasing"
// }

// 4. Exportar para dashboard
const dashboardData = exportarParaDashboard(metricas);

console.log('Charts:', dashboardData.charts);
// [
//   {
//     type: "bar",
//     title: "Tópicos Mais Discutidos",
//     data: [{ topic: "sono", count: 45 }, ...]
//   },
//   {
//     type: "pie",
//     title: "Distribuição de Humor",
//     data: [{ label: "Positivo", value: 0.35 }, ...]
//   }
// ]

console.log('Summary:', dashboardData.summary);
// [
//   "Tópico mais discutido: sono (45 interações)",
//   "Sentimento geral: Predominantemente positivo",
//   "Tempo médio de sessão: 8.5 minutos",
//   "Taxa de retorno: 68%"
// ]

// Renderizar dashboard
renderAnalyticsDashboard(dashboardData);
```

---

## Cenário 8: Geração de Copys com Revisão Humana

```typescript
import {
  gerarPushNotification,
  gerarEmail,
  validarCopyBrand,
  gerarSubjectLineVariante,
} from '@/services/nathia';

// 1. Gerar push notification para novo conteúdo
const push = await gerarPushNotification(
  {
    user_stage: 'postpartum',
    user_name: 'Maria',
    content_title: 'Sono do Recém-Nascido',
  },
  'content_alert'
);

console.log('Push gerada:', push);
// {
//   titulo: "Novo conteúdo para você 🌟",
//   corpo: "Acabamos de publicar: Sono do Recém-Nascido",
//   action: "open_content",
//   requires_human_review: true // SEMPRE
// }

// Validar contra diretrizes da marca
const validacao = validarCopyBrand(push.corpo);
if (!validacao.valid) {
  console.log('Issues:', validacao.issues);
  console.log('Sugestões:', validacao.suggestions);
}

// 2. Enviar para revisão humana (OBRIGATÓRIO)
await sendForHumanReview({
  type: 'push_notification',
  content: push,
  target_audience: 'postpartum_mothers',
  estimated_reach: 1500,
  validation: validacao,
});

// 3. Após aprovação humana, enviar
if (approvedByHuman) {
  await sendPushNotification(push);
}

// 4. Gerar email com A/B testing
const variantA = gerarSubjectLineVariante('weekly_digest', 'A');
const variantB = gerarSubjectLineVariante('weekly_digest', 'B');

console.log('Variante A:', variantA);
// {
//   subject: "Sua semana na Nossa Maternidade",
//   hypothesis: "Tom personalizado e recapitulação"
// }

console.log('Variante B:', variantB);
// {
//   subject: "5 conteúdos que você não pode perder",
//   hypothesis: "Curiosidade e valor numérico"
// }

// Gerar emails para ambas variantes
const emailA = await gerarEmail('weekly_digest', {
  user_name: 'Maria',
  subject: variantA.subject,
  top_content: [...],
});

const emailB = await gerarEmail('weekly_digest', {
  user_name: 'Maria',
  subject: variantB.subject,
  top_content: [...],
});

// Enviar para grupos diferentes (A/B test)
await sendABTestEmails({
  variantA: { email: emailA, percentage: 50 },
  variantB: { email: emailB, percentage: 50 },
});
```

---

## Cenário 9: Fluxo Completo - Usuária Preocupada

Integração de múltiplos módulos em um fluxo real:

```typescript
// Usuária entra no app e inicia conversa
const userId = 'user_123';
const mensagem = 'Estou muito preocupada, meu bebê chora muito e não sei o que fazer';

// 1. TRIAGEM - Analisar sentimento e risco
const [sentimento, risco] = await Promise.all([classificarSentimento(mensagem), detectarRisco(mensagem)]);

// 2. CHAT - Processar mensagem
const contexto = await getUserChatContext(userId);
const resposta = await chatEmpatico(mensagem, contexto);

// 3. ANALYTICS - Registrar interação (anonimizada)
const labels = await extrairRotulos(mensagem);
const dadosAnonimos = anonimizar({
  message: mensagem,
  labels,
  sentiment: sentimento,
  timestamp: new Date(),
});
await saveAnalytics(dadosAnonimos);

// 4. RECOMENDAÇÕES - Oferecer conteúdo relevante
const recomendacoes = await recomendarConteudo(userId, {
  current_mood: sentimento.valence,
  recent_activity: labels.tema,
});

// 5. RESPOSTA INTEGRADA
const respostaCompleta = {
  // Resposta empática da NAT-IA
  message: resposta.resposta,

  // Ações sugeridas
  actions: resposta.acoes,

  // Conteúdos recomendados
  recommended_content: recomendacoes.itens.slice(0, 3),

  // Se houver risco, incluir recursos
  emergency_resources: risco.nivel === 'risk' ? risco.suggested_resources : undefined,

  // Próximo passo
  next_step: resposta.next_step,
};

// 6. Se risco alto, protocolo especial
if (risco.nivel === 'risk') {
  await acionarSOS(userId, {
    mensagem,
    riskAssessment: risco,
    sentimentAnalysis: sentimento,
  });

  // Notificar moderadoras
  await notifyModerators({
    user_id: userId,
    alert_type: 'high_risk',
    message_preview: mensagem.substring(0, 100),
  });
}

// 7. Retornar resposta para UI
return respostaCompleta;
```

---

## Testes de Integração

```typescript
import { healthCheck, initializeNathia } from '@/services/nathia';

// Verificar saúde do sistema
const health = healthCheck();
console.log('Status NAT-IA:', health.status); // "healthy"
console.log('Módulos:', health.modules);
// { chat: true, triagem: true, onboarding: true, ... }

// Inicializar com config customizada
const nathia = initializeNathia({
  moderacao: {
    judgement_threshold: 0.25, // Mais rigoroso
  },
  triagem: {
    threshold_risco_alto: 0.75,
  },
});

console.log('Config válida:', nathia.isValid); // true
console.log('Versão:', nathia.info.version); // "1.0.0"
```

---

## Notas Importantes

### Privacidade

- SEMPRE anonimizar dados antes de analytics
- NUNCA armazenar mensagens completas com identificação
- Validar conformidade LGPD em todos os dados

### Revisão Humana

- Copys SEMPRE requerem aprovação
- Decisões de moderação "reject" podem ser automáticas, mas "review" vai para humanos
- SOS de risco alto notifica equipe imediatamente

### Performance

- Triagem pode rodar em paralelo com chat
- Analytics é processado em background (não bloqueia UI)
- Recomendações podem ser pré-computadas

### Monitoramento

- Health check em cada deploy
- Logs de erros com contexto (sem PII)
- Métricas de qualidade de respostas
