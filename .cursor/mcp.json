{
  "$schema": "https://modelcontextprotocol.io/schema/mcp.json",
  "mcpServers": {
    "neon": {
      "command": "npx",
      "args": [
        "-y",
        "@neondatabase/mcp-server-neon",
        "start"
      ],
      "env": {
        "NEON_API_KEY": "${NEON_API_KEY}"
      },
      "description": "Neon PostgreSQL database integration - Natural language database operations, migrations, and performance analysis (DISABLED - using Supabase now)",
      "enabled": false
    },
    "github": {
      "command": "npx",
      "args": [
        "-y",
        "@github/github-mcp-server"
      ],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}",
        "GITHUB_TOOLSETS": "context,repos,issues,pull_requests,actions"
      },
      "description": "GitHub integration - Repository management, PR/issue automation, CI/CD monitoring",
      "enabled": true
    },
    "memory": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-memory"
      ],
      "env": {
        "MEMORY_FILE_PATH": "${HOME}/.nossa-maternidade-memory.jsonl"
      },
      "description": "Knowledge graph - Preserves project context, architectural decisions, and technical knowledge across sessions",
      "enabled": true
    },
    "sequential-thinking": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-sequential-thinking"
      ],
      "description": "Structured problem-solving - Complex task breakdown and step-by-step reasoning",
      "enabled": true
    },
    "vercel": {
      "command": "npx",
      "args": [
        "-y",
        "@zueai/vercel-api-mcp"
      ],
      "env": {
        "VERCEL_API_KEY": "${VERCEL_API_KEY}"
      },
      "description": "Vercel deployment management - Environment variables, logs, DNS configuration",
      "enabled": true
    },
    "supabase": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres"
      ],
      "env": {
        "POSTGRES_CONNECTION_STRING": "${DATABASE_URL}"
      },
      "description": "Supabase PostgreSQL integration - Database operations, queries, and schema management (uses DATABASE_URL from Supabase)",
      "enabled": true
    }
  },
  "logging": {
    "level": "info",
    "file": ".cursor/logs/mcp.log"
  },
  "security": {
    "notes": [
      "Never commit API keys to version control",
      "All API keys are loaded from environment variables",
      "Ensure .env file is in .gitignore",
      "Rotate API keys regularly",
      "Use read-only tokens when possible"
    ]
  }
}
